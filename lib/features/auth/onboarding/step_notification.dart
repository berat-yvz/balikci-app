import 'package:flutter/material.dart';

/// Onboarding Adım 2 — Bildirim izni.
class StepNotification extends StatelessWidget {
  const StepNotification({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Adım 2: Bildirim izni'));
  }
}
